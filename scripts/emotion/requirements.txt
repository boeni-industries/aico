# Emotion Test Suite Requirements
# Install with: pip install -r scripts/emotion/requirements.txt

requests>=2.31.0  # HTTP client (maintains session automatically)
rich>=13.7.0      # Beautiful console output and tables
pynacl>=1.5.0     # libsodium bindings for encryption
