# Core encryption and security
cryptography
keyring

# Database libraries
libsql
# Note: duckdb and rocksdb can be added when actually needed
# sqlcipher3 removed - not used, libsql handles encryption via PRAGMA

# Configuration management
pyyaml
jsonschema
watchdog
