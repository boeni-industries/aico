enabled: true  # Global kill switch for OpenTelemetry (tracing + metrics)
mode: "prod"  # dev | test | prod ("prod" is an alias for "production")
exporters:
  prometheus:
    enabled: false
  otlp:
    enabled: true
    traces:
      endpoint: "otel-collector:4317"
      insecure: true
